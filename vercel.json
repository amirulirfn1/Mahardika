{
  "framework": "nextjs",
  "rootDirectory": "apps/web",
  "buildCommand": "cd ../../packages/ui && npm ci && npm run build && cd ../../apps/web && npm ci && DATABASE_URL='postgresql://placeholder:placeholder@placeholder:5432/placeholder' npm run build",
  "functions": {
    "src/app/api/**/*.ts": {
      "runtime": "nodejs18.x"
    }
  }
}
