{
  "framework": "nextjs",
  "installCommand": "npm ci --ignore-scripts && cd packages/ui && npm ci && cd ../../apps/web && npm ci",
  "buildCommand": "cd packages/ui && npm run build && cd ../../apps/web && DATABASE_URL='postgresql://placeholder:placeholder@placeholder:5432/placeholder' npm run build",
  "outputDirectory": "apps/web/.next",
  "functions": {
    "apps/web/src/app/api/**/*.ts": {
      "runtime": "nodejs18.x"
    }
  }
}
