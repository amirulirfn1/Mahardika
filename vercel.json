{
  "framework": "nextjs",
  "buildCommand": "npm install --legacy-peer-deps && cd packages/ui && npm run build && cd ../apps/web && npm run build && cd ../..",
  "outputDirectory": "apps/web/.next",
  "devCommand": "cd apps/web && npm run dev"
}
